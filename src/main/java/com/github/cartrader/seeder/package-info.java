/**
 * Used to seed data to the database. Note that this should only be used in dev mode
 * because i've been having issues with importing data using scripts so let JPA handle 
 * persistance
 */
package com.github.cartrader.seeder;